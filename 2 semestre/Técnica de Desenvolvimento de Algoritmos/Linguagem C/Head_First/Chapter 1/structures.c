/*
if(condition)
    <single statement>;

if (condition){
    <statement block>
}

if ((condition 1) && (condition 2))


switch(var){
    case 1:
        <statements>
    break;
    case 'n':
        <statements>
    break;
}

--------break; can be used in every LOOP structure to skip out of it.-------    switch and if are not loops, so it'll not work
--------continue; is used to take you back to the top of the loop body-------


while(condition){           == If the condition on a loop is the value 1, then the loop will run forever. ==
        <statements>
}

do{

}while(condition);

for(var initialize; condition; increment){
    <statements>
}

*/
