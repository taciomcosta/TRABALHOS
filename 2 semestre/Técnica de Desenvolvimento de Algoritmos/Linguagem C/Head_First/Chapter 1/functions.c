/*
int main(){  == returns an INT value ==
}

-------------------------------------------------

int larger(int a, int b)          == this function receives 2 arguments int a and b ==
{
  if (a > b)
    return a;
  return b;
}
int main()
{
  int greatest = larger(100, 1000);         == calling the function here ==
  printf("%i is the greatest!\n", greatest);
  return 0;         == não é necessário, mas é uma prevenção para não dar erro ==
}

----------------------------------------------------------------------
 An ARGUMENT is just a local
variable that gets its value from the code that calls the function.
------------------------------------------------------------------------

VOID FUNCTION  ==  it doesn't have/need a return type. It just does stuff

--------------------------------------------------------------------------

CHAINING ASSIGNEMENTS

y = ( x = 4 );   OR  y = x = 4;      == it assigns the number 4 to x and y as well. ==













*/
