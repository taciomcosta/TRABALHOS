/*
	SYSTEM CALLS

system();
-system() takes a single string parameter and executes it as if you had typed it on the command line:
system("gedit");
system("dir D:")
;

--------------------------------------------------------------------------------------------------------------
exec() FUNCTIONS

list functions: execl(),execlp(),execle()
array functions: execv(), execvp(), execve()

*/