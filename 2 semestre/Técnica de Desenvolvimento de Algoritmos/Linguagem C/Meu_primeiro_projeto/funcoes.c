/*

VARIÁVEIS
-GLOBAIS -> não ficam em nenhum subprograma( main é um subprograma)
-LOCAL -> são declaradas dentro de um subprograma (var x do main é diferente de var x do funcao1)

==========================================================

Funções (Subprogramas)

-> VOID = vazio(não retorna valor)
-> TIPADO = retorna valor


--VOID--

1. Sem parâmetro (PARÂMETROS = VALORES QUE SE PASSA PARA O SUBPROGRAMA PARA MANIPULAÇÃO)
-DECLARANDO
    void name(){
    ...
    }
-CHAMANDO A FUNÇÃO
    name();


1. Com parâmetro

-DECLARANDO
    void name(tipo nome1, tipo nome2){  <-nome1,nome2 são consideradas var locais
    ...
    }

-CHAMANDO A FUNÇÃO
    name(var1,var2); <--argumentos (var1 e var2 podem ser qualquer nome, até mesmo valores)



    1.1 por valor
    1.2 por referência










*/
